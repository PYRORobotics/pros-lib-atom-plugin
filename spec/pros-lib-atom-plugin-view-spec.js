'use babel';

import ProsLibAtomPluginView from '../lib/pros-lib-atom-plugin-view';

describe('ProsLibAtomPluginView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
